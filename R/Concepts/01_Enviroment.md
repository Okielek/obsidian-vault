### `ls()`
Let's you check the contents of your workspace.
### `dir()`
* Lists files in current working directory.
### `library()`
* Loads in library.
## `require()`
Does the same as library however if the package is not installed it returns an error instead.
## `search()`
Checks and prints out currently attached packages.